import React from "react";
import Suggested from "./Suggested";

export default {
  title: "Components/Modal/Suggested/Suggested",
  component: Suggested,
};

const Template = (args) => <Suggested {...args} />;

export const Default = Template.bind({});
